# ft_transcendence - Environment Variables Example
# Copy this file to .env and fill in your actual values

# ============================================
# Google OAuth Configuration
# ============================================
# Get these from: https://console.cloud.google.com/
# 1. Create a project
# 2. APIs & Services → Credentials
# 3. Create OAuth 2.0 Client ID
# 4. Add redirect URI: http://localhost:8080/auth/login/google/callback

GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# Optional: Override default callback URI
# Default: http://localhost:8080/auth/login/google/callback
# GOOGLE_CALLBACK_URI=http://localhost:8080/auth/login/google/callback

# ============================================
# JWT & Security
# ============================================
# Generate a strong random secret for JWT signing
# Example: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-at-least-32-characters-long

# Secret for inter-service API communication
INTERNAL_API_SECRET=your-auth-api-secret-at-least-32-characters-long

# ============================================
# Admin Account
# ============================================
# Default admin credentials (change these!)
LOGIN_ADMIN=admin
PASSWORD_ADMIN=your-secure-admin-password-here

# ============================================
# Frontend Configuration
# ============================================
FRONTEND_BASE_URL=http://localhost:8080

# ============================================
# Services Ports
# ============================================
# Nginx (main entry point)
PORT_NGINX=8080

# Auth service (internal)
PORT_AUTH=3001

# Users service (internal)
PORT_USERS=3002

# 2FA service (internal)
PORT_2FA=3003

# ============================================
# Database Configuration
# ============================================
# SQLite (default for auth service)
# No configuration needed for local development

# ============================================
# OpenAPI / DTO
# ============================================
DTO_OPENAPI_FILE=/app/packages/common/openapiDTO.json

# ============================================
# Monitoring (Optional)
# ============================================
# Prometheus, Grafana, etc.
# Add monitoring-specific variables here if needed

# ============================================
# Development / Production Mode
# ============================================
NODE_ENV=development
# NODE_ENV=production

# ============================================
# Notes
# ============================================
# - Never commit this file with real secrets to Git!
# - Add .env to .gitignore
# - In production, use Docker secrets or environment variables
# - For multiple environments (dev/staging/prod), create separate files:
#   .env.development, .env.staging, .env.production
